Identifier Rules

All JavaScript variables must be identified with unique names (identifiers).

Names can contain letters, digits, underscores (_), and dollar signs ($). (No spaces allowed.)
Names must begin with a letter.
Names can also begin with $ and _.
Names are case-sensitive (y and Y are different variables).
Reserved words (JavaScript keywords) cannot be used as variable names.       