//string are immutable becuaseee we can not change the orginal string , orginal string can't change 

let str = 'hello'
console.log(str);
str[0] = 'H'
console.log(str); //output is same as orginal str string 